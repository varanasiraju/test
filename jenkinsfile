pipeline {
    agent any
    stages {
        stage('Shell commands') {
            steps {
                sh label: '', script: 'sudo chmod 777 raju.sh'
                sh label: '', script: 'sudo sh ./raju.sh'
  
            }
        }
    }
}
