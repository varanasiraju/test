pipeline {
    agent any
    stages {
        stage('Shell commands') {
            steps {
                sh label: '', script: 'sudo sh /Finance/shell/sample.sh'   
            }
        }
    }
}
