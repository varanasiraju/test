pipeline {
    agent any
    stages {
        stage('Shell commands') {
            steps {
                sh label: '', script: 'echo WELCOME TO HPE'
                sh label: '', script: 'echo Please Find Today Date'
                sh label: '', script: 'date'     
            }
        }
    }
}
