pipeline {
    agent any
    stages {
        stage('Shell commands') {
            steps {
                sh label: '', script: 'cd finance/'   
                sh label: '', script: './sample.sh' 
            }
        }
    }
}
